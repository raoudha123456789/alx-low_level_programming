#include "main.h"

/**
 * main - tests function that prints if integer is positive, negative, or zero
 *
 * Return: Always 0
 */
int main(void)
{
    int i;

    i = 0;
    positive_or_negative(i);

    return (0);
}
